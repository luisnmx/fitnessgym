{
  "name": "fitnesgym",
  "version": "1.0.0",
  "description": "FitnesGym — Sistema de Gestión Deportiva e Inventario de Salón (frontend + API Express)",
  "private": true,
  "scripts": {
    "start": "npm --prefix backend start",
    "dev": "npm --prefix backend run dev",
    "test": "node scripts/smoke.js",
    "smoke": "node scripts/smoke.js"
  }
}